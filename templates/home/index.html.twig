{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}

        {# charger mes catégories #}
	<ul class="list-group">
                {% for category in categories %}
		<li class="list-group-item"><a href="{{path('app_get_article_by_category', { id_category: category.id} )}}">{{category.title}}</li>
                {% endfor %}
	</ul>


	{{peiju}}
	<ul>
		{# rediriger vers page Home #}
		<li>
			<a href="{{ path('app_home')}}">
				Home
			</a>
		</li>
		<li>
			<a href="{{ path('app_contact')}}">
				Contact
			</a>
		</li>
	</ul>

{% endblock %}
